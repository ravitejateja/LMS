package com.javatechie.filter;

import org.springframework.cloud.gateway.filter.GlobalFilter;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class GatewayConfigDummy {

	/*
	 * @Bean public GlobalFilter globalFilter(AuthenticationFilter
	 * authenticationFilter) { return (exchange, chain) ->
	 * authenticationFilter.filter(exchange, chain); }
	 */

}